baseURL = "https://example.com/"
languageCode = "en-us"
title = "Dario Amodei — Personal Site (Clone)"
paginate = 10

[params]
  description = "Bio, short posts, essays, research, op-eds, interviews."
  author = "Dario Amodei"

[permalinks]
  posts = "/posts/:slug/"
  essays = "/essays/:slug/"
  "op-eds" = "/op-eds/:slug/"
  interviews = "/interviews/:slug/"

[outputs]
  home = ["HTML","RSS","JSON"]
  section = ["HTML","RSS"]
  page = ["HTML"]
